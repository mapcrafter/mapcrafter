TODO:
	- improve lighting special cases:
		- stairs
	
	- add all blocks:
		- piston extension
		- sign post / wall sign
		- lever
		- tripwire / -hook
		- cobblestone wall
		- heads
		- anvil
		
		- corner stairs?

ideas:
	 - crop the world and render only a specified part
	
	- add a config option to specify which map should be the default map
	  in the browser interface and an option to specify the default view of
	  every map
	- add option to specify a "minimum" zoom level
